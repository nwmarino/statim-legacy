fn fib(n: i64) -> i64 {
  if n == 0 {
    return 0;
  } else if n == 1 || n == 2 {
    return 1;
  } else {
    return fib(n - 1) + fib(n - 2);
  }
}

enum MyEnum {
  A, B, C,
}

fn foo() -> MyEnum {
  return MyEnum::A;
}

fn main() {
  fib(5);
}