fn fib(n: i64) -> i64 {
  let a: bool = n == 0;
  let b: bool = n == 1 || n == 2;
  if a {
    return 0;
  } else {
    if b {
      return 1;
    }
  }

  return fib(n - 1) + fib(n - 2);
}

fn main() -> i64 {
  return fib(1);
}
